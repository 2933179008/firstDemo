<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.handheldInterface.dao.MaterielBindRfidDetailInterfaceDAO">


    <select id="queryMaterielBindRfidDetail" resultType="com.tbl.modules.stock.entity.MaterielBindRfidDetail">

    </select>

    <!-- 获取白糖绑定rfid未绑定数量重量的详情 -->
    <select id="getMaterielBindRfidDetail" resultType="com.tbl.modules.stock.entity.MaterielBindRfidDetail">
        SELECT materiel_bind_rfid_by,materiel_code,materiel_name,batch_rule,amount,weight,unit,rfid,position_id,status
        FROM materiel_bind_rfid_detail
        WHERE delete_flag = 1 AND rfid = #{rfid}
        AND (amount IS NULL OR amount = 0) AND (weight IS NULL OR weight = 0)
    </select>
</mapper>
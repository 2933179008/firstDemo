<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.stock.dao.InventoryTaskDetailDAO">

    <!--获取物料下拉列表-->
    <select id="getSelectPositionList" resultType="java.util.Map">
        select position_code as id,position_name as text from stock group by position_code having count(position_code)>= 1
        <if test='queryString!=null and queryString!=""'>
            and position_name like concat(concat('%', #{queryString}), '%')
        </if>
    </select>

    <!--获取物料下拉列表总条数-->
    <select id="getSelectPositionTotal" resultType="java.lang.Integer">
        select count(1) from stock where 1=1
        <if test='queryString!=null and queryString!=""'>
            and position_name like concat(concat('%', #{queryString}), '%')
        </if>
    </select>

</mapper>
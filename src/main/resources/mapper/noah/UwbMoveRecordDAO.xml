<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.noah.dao.UwbMoveRecordDAO">

	<!-- 根据tag标签更新数据 -->
    <update id="updateUwb">
		UPDATE uwb_move_record
		SET xsize = #{xsize},ysize = #{ysize}, is_move = #{isMove}, scene_code = #{sceneCode},create_time = #{creatTime}
		WHERE tag = #{tag}
	</update>
	<select id="getUwbByUserIp" resultType="com.tbl.modules.noah.entity.UwbMoveRecord">
		SELECT umr.tag,xsize,ysize,scene_code FROM `uwb_move_record` AS umr
		JOIN uwb_forklift AS uf ON (umr.tag = uf.tag) WHERE user_ip =#{userIp}
	</select>
</mapper>
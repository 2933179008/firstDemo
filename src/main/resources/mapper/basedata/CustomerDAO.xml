<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.basedata.dao.CustomerDAO">

    <insert id="savaCustomer" parameterType="com.tbl.modules.basedata.entity.Customer" keyProperty="id">
		insert into  base_customer(id,customer_code,customer_name,customer_Type,
		linkman,telephone,address,mail,remark,create_time) values
		(#{id},#{customerCode},#{customerName},#{customerType},#{linkman},
		#{telephone},#{address},#{mail},#{remark},#{createTime})
	</insert>

    <select id="getAllLists" resultType="com.tbl.modules.basedata.entity.Customer">
        select id,customer_code,customer_name,customer_type,linkman,
        telephone,address,mail,remark
        from base_customer where 1=1
        <if test="list!=null and list.size()>0">
            and id in
            <foreach item="ids" index="index" collection="list"
                     open="(" separator="," close=")">
                #{ids}
            </foreach>
        </if>
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.basedata.dao.DepotPositionDAO">
    <select id="getAllLists" resultType="com.tbl.modules.basedata.entity.DepotPosition">
        select id,depotarea_id,position_code,position_name,base_depot_position.row,
                base_depot_position.column,layer,position_type,
                blend_type,classify,capacity_rfid_amount,length,width,start_x,start_y,end_x,end_y,capacity_weight,remark,create_time,update_time
        from base_depot_position where 1=1
        <if test="list!=null and list.size()>0">
            and id in
            <foreach item="ids" index="index" collection="list"
                     open="(" separator="," close=")">
                #{ids}
            </foreach>
        </if>
    </select>
    <!--依据库位编码查询库位名称-->
    <select id="selectPositionName" resultType="java.lang.String">
        select position_name  from base_depot_position where position_code = #{positionCode}
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tbl.modules.visualization.dao.StockCarDAO">

    <!--获取小车位置信息-->
    <select id="getStockCarPosition" resultType="com.tbl.modules.visualization.entity.StockCar">
        select umr.tag,xsize,ysize,create_time,is_move,scene_code,forklift_name,user_ip
        from  uwb_move_record as umr
        JOIN uwb_forklift as uf on (umr.tag = uf.tag)
        where scene_code = #{sceneCode}
        and create_time between #{formDate} and #{nowDate}
    </select>

</mapper>